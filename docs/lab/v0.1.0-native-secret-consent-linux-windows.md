# `v0.1.0` — consentement natif et mémoire secrète Linux/Windows

## Statut

Implémentation et preuve native fonctionnelle de l'issue `#45` **acquises** au
3 août 2026. Les runs manuels
[`30768351689`](https://github.com/ldesfontaine/your-cloud/actions/runs/30768351689)
et
[`30768749538`](https://github.com/ldesfontaine/your-cloud/actions/runs/30768749538)
restent rouges sous leur ancien oracle, qui exigeait l'absence du canari dans
un dump `LocalDumps`. Leurs observations montrent au contraire qu'une collecte
configurée par un administrateur contient le contrôle du tas et le canari placé
dans la zone `VirtualAlloc`, même enregistrée auprès de
`WerRegisterExcludedMemoryBlock`.
Cette collecte administrateur est hors de la garantie de `#45` ; l'appel WER
reste une défense en profondeur, pas une promesse face à cette autorité.

Le candidat intermédiaire `ae550470bcff08c08624988c17d16db6cb62070a`
corrige l'oracle : le `MDMP` doit contenir le contrôle et le canari, puis
`remove_contents_and_prove_empty` supprime le dump et prouve le répertoire vide,
tandis que les deux inscriptions de registre sont prouvées absentes. Le
répertoire lui-même n'est retiré que par `Drop`, après le verdict. Son run manuel
[`30769440106`](https://github.com/ldesfontaine/your-cloud/actions/runs/30769440106)
a entièrement réussi ses quatre jobs. Ce résultat prouve l'étape intermédiaire,
mais ne peut pas fermer #45. Le correctif suivant
`c8643b0903aee8ad194fb7c34ae6e459c52550a3` remplace ce chemin par
`remove_and_prove_absent` pour retirer le répertoire et le prouver absent avant
verdict.

Le run `workflow_dispatch`
[`30770893733`](https://github.com/ldesfontaine/your-cloud/actions/runs/30770893733)
a ensuite entièrement réussi ses quatre jobs sur le SHA documentaire exact
`b76ded8eccd332dd5e2d7d9a072d7bb09695d049`, qui contient cette correction et
le contrat ci-dessous. Les dialogues, la mémoire protégée, les cas hostiles,
les paquets et les nettoyages Linux/Windows sont donc présentés comme prouvés
dans la limite de `#45`. Les vingt captures et les trois artefacts ont été
inspectés ; aucun secret, dump, paquet ou binaire n'y est archivé.

La présente propagation documentaire modifie seule le SHA après cette preuve.
La fermeture de `#45` exige une ultime matrice verte sur son SHA exact, sans
modification ultérieure de la branche. Son identifiant doit être conservé dans
l'issue `#45`, qui sert de registre non auto-référentiel à cette dernière
porte. Cette exigence d'exactitude ne retire aucune preuve fonctionnelle au run
`30770893733`.

Cette frontière ne ferme ni l'accès SSH personnel `#42`, ni l'intégration
`#35`, ni le palier `#13`, ni la milestone `v0.1.0`.

## Résultat borné par `#45`

Chaque demande `create` ou `replace` fournit au helper un périmètre public
immuable : identifiant natif, cibles et empreintes, étape, action et expiration.
Le helper vérifie son véritable parent et son pair IPC ; une déclaration de
parent forgée, un transport attaquant ou une mutation du périmètre est refusé.
La durée part d'une horloge monotone native, vaut exactement 300 secondes et ne
peut être choisie, renouvelée ou prolongée par le frontend.

Le helper ouvre directement :

- un dialogue GTK3 sous Linux ;
- un dialogue Win32 modal sous Windows, avec contrôle `EDIT | ES_PASSWORD` pour
  une passphrase ou un mot de passe et sans contrôle secret pour un consentement
  `root`.

Ces fenêtres répètent le périmètre exact avant l'acceptation. Elles ne sont ni
du HTML, ni du JavaScript, ni une fenêtre WebView. Le secret accepté entre dans
un `ProtectedSecret` non clonable, non sérialisable et expurgé au débogage,
dont la capacité fixe maximale est de 4096 octets. Le helper détruit ce tampon
à l'annulation, au refus, à l'expiration, à l'erreur et après l'acceptation.
Même après une acceptation, l'événement terminal public reste `Unavailable`
et aucun octet secret ne revient au parent : son
usage SSH et privilégié appartient seulement à `#42`.

## Protections mémoire implémentées

### Linux

`ProtectedSecret` réserve une page avec `mmap`, verrouille la zone avec `mlock`
et l'exclut des dumps ordinaires avec `MADV_DONTDUMP`. Le processus helper
conserve en plus `PR_SET_DUMPABLE=0` et `RLIMIT_CORE=0`. Les sorties contrôlées
écrasent la capacité complète avant `munlock` et `munmap`.

La fixture synthétique garde deux canaris distincts : un contrôle ordinaire
dans le tas et un canari dans la zone protégée. Le scénario `gcore` par défaut
doit retrouver le contrôle ordinaire et ne pas retrouver le canari protégé. Le
scénario durci provoque ensuite un `abort` avec les protections du helper et
doit ne produire aucun core. Ces assertions ont réussi dans la variante Linux
de runs intermédiaires, notamment
[`30765033336`](https://github.com/ldesfontaine/your-cloud/actions/runs/30765033336),
puis dans la matrice complète `30770893733` sur `b76ded8`.

### Windows

`ProtectedSecret` réserve la zone avec `VirtualAlloc`, la verrouille avec
`VirtualLock`, puis appelle `WerRegisterExcludedMemoryBlock`. La destruction
écrase la capacité complète avant `WerUnregisterExcludedMemoryBlock`,
`VirtualUnlock` et `VirtualFree`. L'enregistrement WER constitue une défense en
profondeur best effort. Il ne borne ni un administrateur, ni `LocalDumps`, ni un
débogueur ou dump arbitraire.

La fixture de crash configure une collecte WER locale par exécutable, avec
`DumpType=0`, `CustomDumpFlags=0x321`, un seul fichier et un répertoire
temporaire borné. Ces flags demandent `DataSegs`, `UnloadedModules`,
`ProcessThreadData` et `PrivateReadWriteMemory` : le contrôle ordinaire et la
zone `VirtualAlloc` en `PAGE_READWRITE` restent donc candidats à l'inclusion.
La fixture caractérise explicitement la frontière administrateur : elle vérifie
la signature `MDMP` et exige que le contrôle ordinaire **et** le canari protégé
soient présents. Cette présence n'est pas une fuite revendiquée comme couverte
par le produit ; elle empêche au contraire de présenter
`WerRegisterExcludedMemoryBlock` comme une garantie. Après l'observation, le
test doit supprimer le dump et son répertoire, retirer la clé `LocalDumps` et
l'exclusion `AeDebug` propres à la fixture, puis prouver ces quatre absences
avant verdict.
Le processus est lancé avec `CREATE_DEFAULT_ERROR_MODE` et refuse de poursuivre
si `SEM_NOGPFAULTERRORBOX` reste actif, car ce mode hérité interdit précisément
l'invocation de WER.

Cette preuve Windows est acquise dans `30770893733`. Les runs sans dump ne
caractérisent pas cette frontière. `30768351689` puis `30768749538` montrent la présence du
canari sous `LocalDumps` ; ils sont rouges parce que leur ancien oracle exigeait
l'inverse. `30768749538` a de plus paniqué avant la preuve de nettoyage. Ces
runs fixent donc le contrat corrigé sans devenir une preuve de fermeture. Le
test `administrator_local_dump_is_outside_the_wer_exclusion_contract` réussit
ensuite sur `b76ded8` : son ordre bloquant retire et prouve absent le répertoire
avant de vérifier les deux inscriptions de registre puis de rendre son verdict.

## Exécutions diagnostiques et preuves exactes

| Run | SHA | Résultat utile | Portée ou limite |
|---|---|---|---|
| [`30764166496`](https://github.com/ldesfontaine/your-cloud/actions/runs/30764166496) | `4aa03b287c55da734987c0aef241ce6428ec24f4` | le garde générique réussit | le garde Plumber refuse un comptage attendu devenu obsolète après l'évolution du workflow ; les variantes natives sont sautées |
| [`30764301331`](https://github.com/ldesfontaine/your-cloud/actions/runs/30764301331) | `a113219d342fba40ee3c49f4bd84f89885d65dd9` | Plumber et garde générique réussissent | les variantes révèlent des erreurs de compilation multi-OS avant les scénarios natifs |
| [`30765033336`](https://github.com/ldesfontaine/your-cloud/actions/runs/30765033336) | `3b1635b4c35e2da63ba81158753791c1aec9b831` | gardes et variante Linux réussissent, y compris la preuve `gcore`/core ; Windows atteint la fixture WER | aucun dump WER Windows n'est créé ; la variante Windows échoue avant les dialogues, le paquet et l'artefact final |
| [`30766208361`](https://github.com/ldesfontaine/your-cloud/actions/runs/30766208361) | `08023da17c7d0df63309ff2adcbb1d89d4c06410` | gardes et Linux réussissent ; la fixture ajoute l'`AutoExclusionList` WER et une exception | Windows ne crée toujours aucun dump ; l'absence de dump interdit de conclure sur l'exclusion mémoire |
| [`30767224411`](https://github.com/ldesfontaine/your-cloud/actions/runs/30767224411) | `87f3bd51af01cff5d78a2b8cc846da607db14279` | les gardes réussissent et la fixture Linux atteint ses assertions ; Windows emploie `RaiseFailFastException` | Windows ne crée toujours aucun dump ; l'analyse identifie ensuite le mode d'erreur parent hérité comme blocage possible de WER |
| [`30767609914`](https://github.com/ldesfontaine/your-cloud/actions/runs/30767609914) | `d0de8159134410f125eee485ecfccffa055f41eb` | les deux gardes réussissent ; la fixture neutralise l'héritage du mode d'erreur | les deux variantes échouent au formatage Rust avant le scénario de crash ; aucun verdict WER n'est obtenu |
| [`30768351689`](https://github.com/ldesfontaine/your-cloud/actions/runs/30768351689) | `f5045ddbacd5c848ffc5b1ebdc1178ce49eeeebb` | le formatage réussit et Windows produit un dump `MDMP` contenant le contrôle ordinaire et le canari | l'ancien oracle exige l'absence du canari et rend le run rouge ; l'observation caractérise `LocalDumps` administrateur, hors garantie |
| [`30768749538`](https://github.com/ldesfontaine/your-cloud/actions/runs/30768749538) | `5cceb7835d1b80b8c425765068ebfd4d67202a51` | le dump WER personnalisé `DumpType=0`, `CustomDumpFlags=0x321` est un `MDMP` stable contenant le contrôle du tas et le canari | l'ancien oracle rend le run rouge et panique avant la preuve de nettoyage ; seuls `Drop` best effort et le runner éphémère bornent alors les restes |
| [`30769440106`](https://github.com/ldesfontaine/your-cloud/actions/runs/30769440106) | `ae550470bcff08c08624988c17d16db6cb62070a` | `SUCCESS` 4/4 : `MDMP`, contrôle et canari présents, dump supprimé, répertoire prouvé vide et deux inscriptions de registre absentes ; trois artefacts inspectés | le répertoire n'est retiré qu'au `Drop` après verdict, donc ce SHA constitue une preuve intermédiaire et ne peut pas fermer #45 |
| [`30770893733`](https://github.com/ldesfontaine/your-cloud/actions/runs/30770893733) | `b76ded8eccd332dd5e2d7d9a072d7bb09695d049` | `SUCCESS` 4/4 : même observation, `remove_and_prove_absent`, deux inscriptions absentes avant verdict, matrice native, paquets et trois artefacts inspectés | preuve fonctionnelle de #45 ; l'ultime rejeu du SHA de propagation documentaire doit être enregistré dans l'issue avant fermeture |

Les runs rouges restent utiles pour reproduire les défauts du harnais et
expliquer les corrections. Aucun n'est transformé rétroactivement en preuve de
fermeture. Le succès `30769440106` prouve seulement le contrat intermédiaire de
son SHA ; `30770893733` prouve le contrat corrigé complet sur `b76ded8`.

### Résultat inspecté de `30769440106`

Le run a rendu `SUCCESS` du `2026-08-02T22:08:15Z` au
`2026-08-02T22:36:00Z`. Ses quatre jobs sont verts :

| Job | ID | Résultat ou durée connue |
|---|---:|---|
| Plumber | `91553861038` | `SUCCESS` |
| contrôles génériques | `91553861065` | `SUCCESS` |
| Windows | `91553980937` | `SUCCESS` en 26 min 27 s |
| Linux | `91553980938` | `SUCCESS` en 15 min 24 s |

Le run publie exactement trois artefacts :

| Artefact | ID | Taille | Empreinte GitHub | Empreinte du JSON |
|---|---:|---:|---|---|
| smoke Windows | `8840335490` | 288462 octets | `sha256:4d88cb47143c921e9ae6bd12f07387ea92eda78b5ea1b69fa01b0bca4056d24e` | `b33189dfaab1c971222a3ccc65b5ef2532657e5ba7c7ae185be74c7a01281900` |
| smoke Linux | `8840204853` | 296937 octets | `sha256:f2694d8ec926d92e085327ffcb76e862687160f0da9d0c326319e8545dce4936` | `ab150f2527a35633196c2e4cef15d2cb90fb8ef78c2a366dd4cbb7873f3e3303` |
| rapport Plumber | `8840019185` | 1855 octets | `sha256:9b007bb48ff01347f42b204900d071b976fdcad6f069e84fc846eb68f44f1ba2` | `339c545d3f8bdc0e9109ef76d38a592e069d9ce4a7e570ab700beb539c39a747` |

Les smokes Linux et Windows contiennent chacun exactement onze fichiers : un
JSON et dix PNG, dont neuf vues de l'interface et un consentement natif. Les
vingt captures ont été inspectées visuellement ; elles sont cohérentes et ne
montrent aucun secret. Le scan des trois artefacts ne retrouve aucun `MDMP`,
core, clé, paquet, binaire, canari ou sentinelle.

Le JSON Windows lie le SHA `ae550470bcff08c08624988c17d16db6cb62070a`
et le run, consigne des verrous sources dont les empreintes CRLF Windows sont
cohérentes, relie le MSI, le helper et l'exécutable vérifiés, puis rend le
nettoyage `pass`. Le JSON Linux rend également `pass` ; son lien au SHA et au
run vient de la métadonnée GitHub et non de champs absents de ce rapport.

### Résultat inspecté de `30770893733`

Le run a rendu `SUCCESS` du `2026-08-02T22:48:02Z` au
`2026-08-02T23:14:31Z`. Ses quatre jobs sont verts :

| Job | ID | Résultat ou durée |
|---|---:|---|
| contrôles génériques | `91557743005` | `SUCCESS` en 55 s |
| Plumber | `91557743018` | `SUCCESS` en 21 s |
| Windows | `91557829499` | `SUCCESS` en 25 min 26 s |
| Linux | `91557829507` | `SUCCESS` en 15 min 43 s |

Le run publie exactement trois artefacts :

| Artefact | ID | Taille | Empreinte GitHub | Empreinte du JSON |
|---|---:|---:|---|---|
| smoke Windows | `8840757379` | 288424 octets | `sha256:8ee952ba6a265e4ad94289eb265cd19ab4ab5bef472f4d4d2dcbb6d56c38b973` | `a2efa0a548d131e1000b4f03b7a7a6a511f45f2ec42e39640f2148dc32a8dfe0` |
| smoke Linux | `8840658837` | 296937 octets | `sha256:56e904626e35c06fefe4966850b307f2dbc97eb075b83831f5fa15845e4cf58f` | `ab150f2527a35633196c2e4cef15d2cb90fb8ef78c2a366dd4cbb7873f3e3303` |
| rapport Plumber | `8840483746` | 1854 octets | `sha256:98499038c307b2d3dddeca46fc4195dbbf51b50a27cbe1a33b03609cfef95763` | `629dc3b0e73b192b92b9b89fe5d4bd9cce6ce18041cd3c8283a27511ed0c5ff2` |

Les smokes contiennent chacun exactement un JSON et dix PNG. Les vingt
captures ont été inspectées : les deux dialogues natifs montrent seulement la
cible synthétique et le périmètre public ; aucune capture ne montre un secret.
Le scan des trois artefacts ne retrouve aucun `MDMP`, core, clé, paquet,
binaire, canari ou sentinelle. Le rapport Plumber vaut `A`, `100/100`, sans
finding.

Le JSON Windows lie exactement `b76ded8`, le run et un checkout propre. Les
empreintes de `package-lock.json` et `Cargo.lock` concordent avec la
matérialisation CRLF Windows des blobs du SHA ; le MSI, la Console et le helper
installé concordent, puis le
nettoyage bloquant rend `pass` avec les huit catégories attendues absentes. Le
JSON agrège le matériel WER sous `temporary-security-material` : l'absence
distincte du répertoire, de `LocalDumps` et d'`AeDebug` avant verdict est
prouvée par l'ordre bloquant du test et son résultat vert, pas par ce seul JSON.
Le rapport Linux rend `pass` et reste volontairement lié par la métadonnée
GitHub. Aucun des deux rapports ne prétend un succès métier.

## Preuve de fermeture et registre exact

La fermeture exige dans un même run manuel :

1. `Contrôles génériques` et `Politique Plumber` ;
2. tous les tests release du workspace Linux et Windows ;
3. les refus de parent, pair, périmètre, renouvellement et expiration ;
4. les dialogues GTK3 et Win32 nominaux et hostiles ;
5. la destruction observable des buffers propres au produit ;
6. la preuve Linux `gcore` et l'absence de core durci ;
7. la caractérisation WER Windows dans le dump personnalisé incluant
   `PAGE_READWRITE` : contrôle ordinaire et canari présents, puis suppression
   prouvée du dump, absence de son répertoire avant verdict et absence des deux
   inscriptions de registre ;
8. les builds, installations, lancements, absences de listener et nettoyages ;
9. les artefacts expurgés Linux et Windows, exactement onze fichiers chacun —
   un JSON, neuf vues et un consentement natif PNG — reliés au run et inspectés
   sans paquet, exécutable, certificat, clé privée ou secret synthétique. Sous
   Linux, ce lien vient de la métadonnée GitHub, pas de champs ajoutés au JSON.

`30770893733` satisfait ces neuf points sur `b76ded8`, et ses artefacts sont
inspectés. La propagation de ces faits constitue la seule modification
ultérieure. L'issue `#45` doit lier l'ultime run entièrement vert du présent SHA
documentaire ; aucune ligne ne doit ensuite changer avant fusion. Ce registre
GitHub évite de créer une impossible auto-référence du rapport à son propre SHA.

## Justification de sécurité et limites

- **Actifs et menace** : passphrase de clé, mot de passe `sudo` et consentement
  `root` pourraient être copiés par la WebView, l'IPC, un log, un dump ou un
  processus non lié à l'opération.
- **Choix** : une fenêtre système dans le helper séparé, un périmètre immuable,
  une échéance monotone non renouvelable, un tampon natif borné et des défenses
  en profondeur propres à chaque OS. Sous Windows, l'enregistrement WER n'est
  pas élevé au rang de garantie. Une seconde WebView, le binaire Console comme
  helper et un secret transmis au frontend restent écartés.
- **Moindre privilège** : le helper reçoit seulement ce consentement et détruit
  le secret sans l'utiliser tant que `#42` n'apporte pas une capacité SSH
  exacte. Cette valeur sûre par défaut applique réduction de surface,
  séparation des responsabilités et défense en profondeur recommandées par
  OWASP ; elle contribue proportionnellement aux mesures NIS2 de cryptographie,
  contrôle d'accès, développement sûr et mesure d'efficacité sans déclarer une
  conformité.
- **Risque résiduel** : GTK3, Win32 et le système peuvent conserver des copies
  internes non contrôlées par `ProtectedSecret`. La preuve ne couvre ni `root`,
  ni un administrateur, ni `SeDebugPrivilege`, ni une IME ou couche
  d'accessibilité hostile, ni un dump noyau, ni une machine déjà compromise.
  `mlock` et `VirtualLock` réduisent l'exposition à l'échange ; ils ne prouvent
  pas l'absence universelle de copie.

## Ce que `#45` ne prouve pas

- aucune connexion, authentification ou commande SSH ;
- aucun agent SSH, fichier de clé chiffré ou KDF bcrypt ;
- aucun `sudo`, accès `root` ou descendant privilégié métier ;
- aucun audit d'hôte, paquet serveur installé ou Controller autonome ;
- aucun succès d'amorçage ou de remplacement ;
- aucune preuve contre un débogueur arbitraire, un administrateur, un dump
  noyau, une IME, une couche d'accessibilité ou les copies internes des
  widgets.

## Environnement et nettoyage

Les tentatives ci-dessus utilisent uniquement des runners GitHub hébergés et
jetables avec des canaris synthétiques. Elles ne créent aucune VM, aucun réseau
ni snapshot `labctl`. Dans `30768749538`, le panic sur le canari a précédé le
nettoyage explicite : seul le `Drop` best effort de la fixture puis la
destruction du runner éphémère ont borné les restes. Ce run ne prouve donc pas
l'absence finale du dump, des clés de registre ou du répertoire temporaire et
ne peut servir de preuve de nettoyage produit. Sur `ae550470`, le rattrapage de
panic exige la suppression du dump, le répertoire prouvé vide, la clé
`LocalDumps` et l'exclusion `AeDebug` prouvées absentes ; le `Drop` retire
seulement ensuite le répertoire, hors du verdict. `c8643b0` ajoute la preuve
requise de son absence avant verdict avec `remove_and_prove_absent` ; le run
`30770893733` la valide dans le même test que les deux absences de registre.
Aucun passage n'a ouvert ou conservé une topologie LAB locale.
