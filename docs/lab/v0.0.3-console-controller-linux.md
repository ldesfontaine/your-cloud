# Preuve LAB `v0.0.3` — porte Linux Console–Controller

> Résultat : **PORTE LINUX RÉUSSIE** le 20 juillet 2026 sur la branche
> `console-controller`, à `HEAD`
> `afb31e83ba6b6cbec0211e054d47094920f066fa`, depuis un checkout propre.
> Le candidat reste un artefact de preuve LAB signé par une identité synthétique,
> mais ses sources sont maintenant rattachées à ce commit exact. Windows est
> autorisé et en préparation ; il n'a pas encore été exécuté, donc `v0.0.3`
> reste ouverte et ne peut pas être déclarée terminée.

## Résultat observable

Le paquet Linux installé a créé puis déverrouillé son coffre natif, remplacé sa
phrase locale, associé deux Controllers séparés et consulté deux machines de la
seule infrastructure A. Controller B a conservé un inventaire vide et aucune
donnée, identité ou session de B n'est apparue dans A.

Le chemin nominal réellement exécuté était :

```text
Console installée · lab-console · réseau administration 192.168.241.0/24
  `- HTTPS TLS 1.3, appareil mTLS et humain distincts
       `- Controller A · lab-coordinateur · 192.168.242.103:9443
            `- lecture mTLS privée filtrée
                 `- Relay · lab-machine-1 · 192.168.243.153:8444
                      |- ingestion Daemon 1 · même hôte · :8443
                      `- ingestion Daemon 2 · lab-machine-2 · :8443
```

Controller A portait `controller_id`
`9ae3ff9b-e613-4bd5-9d70-b16d1e0b3a11` et `infrastructure_id`
`c71d0c82-7b20-4b4b-a16e-9334ea151cb4`. Le Controller B éphémère portait des
identifiants, une IPv4, un état, des autorités et des sessions distincts. Il a
été retiré après la preuve ; Controller A et les deux Daemons restent le nominal
final annoncé.

Cette exécution n'a lancé ni Ansible métier, SSH d'action, plan appliqué,
Auxiliaire, WireGuard, OCI, téléphone, navigateur public, passerelle Web,
Proxmox, OpenStack, worker, IaC, série temporelle, plugin libre, scan LAN,
renouvellement automatique ou élection Relay.

## Inventaire et placement

`tools/labctl list` a précédé toute mutation. Les six cibles actives provenaient
de `your-cloud/labctl`, topologie `v1-full`, avec les gabarits décidés :

| VM | Rôle exécuté | Adresse pendant le run |
|---|---|---|
| `lab-console` | installation et exécution du `.deb`, WebDriver externe | `192.168.241.193` |
| `lab-console-recovery` | runner de tests, rebuild exact et client hostile | `192.168.241.140` |
| `lab-coordinateur` | Controller A, puis Controller B éphémère | `192.168.242.103`, alias B `192.168.242.104` retiré |
| `lab-gateway` | routage et filtrage seulement | `192.168.241.151`, `192.168.242.182`, `192.168.243.144` |
| `lab-machine-1` | Daemon 1 et Relay, processus et autorités séparés | `192.168.243.153` |
| `lab-machine-2` | Daemon 2 | `192.168.243.158` |

Le laptop a servi à Git, à l'édition, aux contrôles statiques autorisés, à
l'empaquetage de listes positives non sensibles et au pilotage `labctl`. Tous
les tests, builds, processus produit, serveurs et preuves ont été exécutés dans
le LAB. Aucun contenu de `keys.txt` ou de `/srv/infra/secrets/` n'a été lu,
affiché ou copié.

## Candidat Linux exact

Le troisième candidat est celui qui a porté la preuve fonctionnelle complète.
Après l'autorisation de commit, le candidat exact ci-dessous a été reconstruit,
signé, vérifié, installé et lancé. Son manifeste annonce explicitement
`release_status: candidate-exact-commit`, le HEAD attendu et un worktree propre.

| Élément | Valeur observée |
|---|---|
| commit source | `afb31e83ba6b6cbec0211e054d47094920f066fa` ; checkout superficiel propre, archive SHA-256 `cb6c71673383ca497a1acd7706b75dd3c1b35960e944240641cce49318d80fe9` |
| source Console | 26 fichiers ; arbre SHA-256 `dc035167c068fae82eb18c8e1013f0f4b8694da904cdca2efb4164648c14878a` |
| verrou frontend | SHA-256 `9bdee074967cd50be57fb8efab8e4465a770b945e096100b7199a4b71f93f903` |
| verrou Rust | SHA-256 `dd2b8403c9a7d0bcdc6ce8b5259b3ad40a8cba8c7f3a07ccd6874fe467012546` |
| paquet | `your-cloud-console_0.0.3_amd64.deb`, 5 736 242 octets, SHA-256 `74f940ad9cf9696c2402f04cdf9681b09a155b8912c9790f0465b841117b06ad` |
| SBOM | CycloneDX 1.6, 659 composants, 255 302 octets, SHA-256 `4a18cbe5f932e29468ba4d7a70cf3befab5c8ccb5bd50ff06be8a6af4fd11317` |
| manifeste | SHA-256 `9d616ad3633f80c4a2fe74c57719b4d932e74b77267366a5e9480d810a1034fd` |
| signature détachée | SHA-256 `146f7d7767f56175b1c9fb5640491e9d115581a7d31888083b87c36ee98ab3cd` |
| signataire LAB | OpenPGP Ed25519 synthétique `913F 5A85 5B47 9354 713E 3246 6F03 186E 2A88 9846` |

La signature du manifeste et l'empreinte attendue ont été vérifiées avant
l'installation. La clé privée synthétique de signature a ensuite été détruite.
Cette preuve valide le mécanisme de signature, pas une identité publique ni la
provenance d'une future release.

Le rebuild exact a utilisé `lab-console-recovery`, Debian 13 `x86_64`, Node
`20.19.2`, npm `9.2.0`, Rust et Cargo `1.94.1`, Tauri CLI `2.11.4` et
`dpkg-deb` `1.22.22`. Après vérification des cinq empreintes, de la signature,
du signataire attendu et du manifeste, `lab-console` a confirmé le paquet
installé `your-cloud 0.0.3` et `/usr/bin/your-cloud-console`. Le processus natif
est resté actif pendant les douze secondes du lancement virtuel prévu, puis le
timeout l'a arrêté avec le statut `124`; aucun nouveau listener produit n'est
apparu.

## Gates source et build

Un lot Go à liste positive, sans secret, a été transféré dans
`lab-console-recovery`. Le premier rejeu a trouvé l'absence de `go.sum` pour
`golang.org/x/text v0.29.0`. `go mod tidy` a été exécuté dans le runner, le seul
`go.sum` produit a rejoint le worktree, puis un nouveau lot a été créé.

Le lot final mesurait le SHA-256
`18e7e4c50aab32725796e5211e980fa2e772f8683d0716673f0ddf91febc5760`.
Les contrôles réellement exécutés étaient :

```text
go test -mod=readonly -count=1 ./...   -> PASS, tous les paquets
go vet -mod=readonly ./...             -> PASS
cargo test --release                   -> PASS, 6 réussis, 1 test LAB ignoré
npm test                               -> visual-contract: PASS
npm run build                          -> PASS, tsc --noEmit puis Vite 8.1.5
```

Le test Rust ignoré est précisément le scénario vivant qui exige une fenêtre
d'appairage et un Controller isolé. Il a été exécuté séparément contre le
Controller C de preuve : `1 passed`, `0 failed`, en `12,24 s`.

Le frontend de production a transformé 1 788 modules. Les fontes Inter et IBM
Plex Mono sont restées embarquées ; le bundle rendu contenait environ
`229,48 kB` de JavaScript et `12,35 kB` de CSS avant compression.

## Parcours Console Linux

Sur le paquet installé, les assertions fonctionnelles ont produit :

```text
console_functional=initialize result=pass secret_output=redacted
console_functional=change-phrase result=pass secret_output=redacted
console_functional=pair result=pass secret_output=redacted
console_functional=configure result=pass secret_output=redacted
console_functional=multi-controller result=pass secret_output=redacted
```

- la phrase générée contenait exactement six mots et le code global avait une
  forme Base32 canonique ; leur confirmation a précédé la création durable ;
- après création, aucun secret n'est resté dans le DOM, les champs mot de passe,
  `localStorage` ou `sessionStorage` ;
- l'ancienne phrase a été refusée après remplacement et la nouvelle a rouvert
  l'unique coffre ;
- le coffre natif était placé sous le répertoire de données de l'application,
  avec répertoire `0700` et fichiers `0600`, pas sous l'état WebKit ;
- le port temporaire `9444` s'est fermé après l'appairage et seul `9443` est
  resté actif ;
- Controller A a reçu un inventaire de deux machines ; Controller B est resté à
  zéro, `Relay indisponible`, sans machine A affichée ; le retour vers A a rendu
  exactement les deux machines attendues.

Les feuilles d'appairage ont transité directement entre VM par SFTP forcé,
avec empreinte d'hôte épinglée, clé temporaire, règle réseau à source exacte et
comparaison SHA-256. Clés, autorisations, règles, routes et feuilles temporaires
ont été retirées après chaque transfert.

## API hostile et séparation des autorités

Un Controller C dédié a été créé puis détruit pour éviter d'altérer A ou B. Le
test Rust vivant lancé depuis `lab-console-recovery` a exécuté le parcours
nominal puis les refus suivants :

- appel sans certificat client et certificat client auto-signé étranger : TLS
  refusé avant toute réponse HTTP ;
- identité d'appareil remplacée : ancien certificat refusé après rotation ;
- code de récupération : rotation globale menée jusqu'à l'époque 2 ;
- authentification humaine invalide ;
- route inconnue `404`, mauvaise méthode `405`, query `400`, schéma `400`,
  corps surdimensionné `413` et mauvais `Host` `403` ;
- logout puis rejeu du jeton : `401` ;
- lecture Relay indisponible : projection bornée, sans machine fabriquée.

Après cette matrice, l'identité active de C était en révision 2, une identité
était révoquée et trois reçus étaient persistés. Son état, son alias IP, sa
feuille, ses identités et ses règles temporaires ont été détruits.

Les tests Go ont couvert en plus les entrées et états difficilement provoqués
sans corruption volontaire du nominal : fichiers absents, modes et liens
hostiles, JSON strict, révisions, publication atomique, régression du cache,
bornes de fraîcheur à la nanoseconde, libellés Unicode, tailles et projection
vide. Ces tests automatisés ne sont pas présentés comme une exécution vivante de
chaque crash ou temporisation du contrat complet.

## Frontières réseau observées

Depuis la Console hostile du segment d'administration, `nmap` a vu :

- `192.168.242.103:9443` et l'ancien alias B `:9443` ouverts, ce qui est attendu
  sur ce réseau privé ;
- `9444` fermé hors fenêtre ;
- SSH filtré.

Depuis `lab-machine-1` et `lab-machine-2`, les deux origines Controller `:9443`
étaient filtrées ou expiraient. `lab-gateway` n'avait aucune redirection publique
pour `9443` ou `9444`. La seule règle produit interzone finale autorise
`192.168.242.103` vers `192.168.243.153:8444`; les autres paquets gardent la
politique de refus de la passerelle. La connectivité du segment privé ne vaut
donc ni identité d'appareil ni identité humaine.

Cette preuve n'a pas envoyé un scan depuis Internet public. Elle démontre
l'absence de publication par la passerelle LAB et le filtrage entre les zones
LAB, pas le comportement d'un pare-feu ou routeur réel hors de cette topologie.

La Console lancée normalement, sans WebDriver, n'a ouvert aucun listener. Sous
WebDriver, les ports loopback `4444`, `4445` et un port WebKit aléatoire ont été
observés ; ils appartiennent à l'instrumentation d'automatisation et ne sont pas
présents lors du lancement direct. Aucun port produit Console n'était exposé sur
le LAN dans l'un ou l'autre cas.

## Relay, fraîcheur, lacune et redémarrages

Le cycle vivant a affirmé les états suivants dans l'interface installée :

```text
Relay disponible       -> deux machines Récentes
Relay arrêté           -> Relay indisponible, deux machines conservées
Daemon 2 arrêté > 90 s -> machine 1 Récente, machine 2 Ancienne
Daemon 2 repris        -> deux machines Récentes
Relay redémarré        -> deux Daemons republient, deux machines Récentes
```

Une lacune réelle de `lab-machine-2`, séquence `10`, `dropped_count=1`, est
restée visible dans son détail avant, pendant et après la donnée ancienne, la
reprise du Daemon et le redémarrage du Relay. Elle n'est jamais devenue une
machine ou un faux score global.

Le premier arrêt du Relay avait révélé que son unité `systemd-run` transitoire
et ses credentials sous `/run` n'étaient pas redémarrables. Le correctif a
ajouté une unité persistante et un générateur LAB qui crée quatre autorités
synthétiques séparées : serveur Relay, clients Daemon, serveur lecteur et
clients Controller. Le générateur a rendu
`runtime_pki=generated authorities=4 ca_private_keys=persisted:0`. Aucune clé
privée d'autorité n'a été persistée ou transférée.

Après installation des seules feuilles nécessaires, le Relay a redémarré par
`systemctl restart your-cloud-relay.service`, réouvert uniquement
`192.168.243.153:8443` et `:8444`, relu son état et reçu de nouvelles
observations des deux Daemons. Les sources temporaires de credentials ont été
déchiquetées après ce rejeu ; les feuilles installées nécessaires au nominal A
restent protégées sur leurs hôtes.

## Moindre privilège observé

Controller et Relay fonctionnent sous `DynamicUser=yes`, sans capacité Linux,
avec `NoNewPrivileges=yes`, état `0700` et fichiers `0600`. Les deux Daemons
emploient aussi un utilisateur dynamique et aucune capacité. Les niveaux
`systemd-analyze security` observés étaient :

| Unité | Niveau d'exposition observé |
|---|---|
| `your-cloud-controller.service` | `3.0 OK` |
| `your-cloud-relay.service` | `3.0 OK` |
| `your-cloud-daemon.service` | `3.1 OK` |

Ces scores sont un diagnostic systemd de cette configuration, pas une preuve de
sécurité globale. Le Daemon hérité ne fixe notamment pas encore `UMask=` dans
son unité et le diagnostic pénalise aussi l'accès réseau requis. Les décisions
sont cadrées selon les actifs, le moindre privilège, les autorités séparées et
les entrées bornées du [contrat](../objectifs/v1/CONTRAT-V0.0.3.md), avec les
sources ANSSI et OWASP qui y sont liées. Cette exécution ne revendique aucune
conformité globale ANSSI, OWASP, NIS2 ou WCAG.

## Interface et accessibilité instrumentée

Les captures et rapports restent dans `lab-console`, sous les dossiers de run
non versionnés. Leur export vers le laptop a été refusé par la garde
d'environnement et n'a pas été contourné.

Le paquet exact a passé en thèmes clair et sombre :

- écran `Accès local` à `1280 x 800`, `640 x 560` et texte à 200 % ;
- les six vues authentifiées `Infrastructures`, `Association ou récupération`,
  `Synthèse`, `Parc`, `Observations`, `Profil et sessions`, aux deux tailles et
  avec texte à 200 % ;
- aucune vue n'a produit de défilement horizontal ;
- chaque vue avait un contrôle visible d'au moins `44 px`, un focus clavier
  visible et aucune ressource `http:` ou `https:` chargée par la WebView ;
- une demande de fenêtre `500 x 400` est restée bornée au minimum
  `640 x 560`.

Les deux rapports authentifiés portaient les SHA-256 :

- sombre : `5fa83a46db0e0aeda11e68798042bedc06c723d1f94aedd1a7abdd408105ff71` ;
- clair : `87fc9aac06d1dbf74795bfbe7489daff36898c3641ca7ca30f5889f731c36764`.

Cette instrumentation prouve les assertions mesurées, pas une navigation
complète au lecteur d'écran ni une certification d'accessibilité. Les contrastes
contractuels restent portés par les tokens et les contrôles statiques ; ils
n'ont pas fait l'objet d'un audit WCAG indépendant dans ce run.

## Incidents conservés

| Incident | Conclusion et correction |
|---|---|
| candidat 2 : coffre cherché sous un chemin relatif au binaire | candidat rejeté ; candidat 3 place le coffre dans le répertoire de données natif |
| un build sous `/tmp` a été tué par le runner | aucun artefact partiel retenu ; build relancé dans un emplacement borné |
| saisie PEM multi-ligne incomplète par WebDriver | remplissage React atomique ajouté au pilote ; appairage rejoué |
| premier critère `openssl s_client` faux en TLS 1.3 | résultat écarté ; vraie requête `curl` sans certificat utilisée |
| premier transfert vers une machine : mauvaise IP source supposée | règle retirée ; source réelle vérifiée puis transfert rejoué avec empreinte identique |
| pilote Tauri lancé sans session D-Bus ou affichage complet | aucun résultat produit retenu ; `dbus-run-session` et `xvfb-run` explicités |
| unité Relay transitoire impossible à redémarrer | incident produit retenu ; unité persistante et credentials durables séparés ajoutés puis redémarrés |
| anciens credentials Daemon lors de la transition | deux erreurs TLS visibles à `03:44:33Z` et `03:44:34Z` ; credentials remplacés, livraisons reprises |
| `go.sum` absent | checkout propre correctement refusé ; `go.sum` généré dans le LAB, nouveau lot et gates verts |
| archives datées de 1 à 3 secondes dans le futur | faible dérive laptop/runner consignée ; aucune tolérance produit modifiée |
| lancement direct headless : avertissements de portail XDG | aucun listener produit et processus actif ; limite de l'affichage virtuel consignée |
| checkout exact extrait avec l'UID du laptop | Git l'a refusé comme propriétaire douteux ; propriété corrigée vers `root:root` dans la VM avant toute exécution |
| schémas Tauri générés non ignorés | manifeste exact fermé sur worktree sale ; `/console/src-tauri/gen/` ajouté aux sorties ignorées, nouveau commit et rebuild complet |
| test Rust lancé avant la génération de `dist/` | macro Tauri fermée sur frontend absent ; aucun binaire retenu, ordre build frontend puis test Rust rejoué avec succès |
| métadonnée d'un crate non-Linux absente du cache | SBOM hors ligne fermé ; `cargo fetch --locked` a téléchargé les seules versions et empreintes du verrou, puis le SBOM a été généré hors ligne |
| tube `labctl ssh` VM vers VM vide | le `ssh -n` intentionnel de `labctl` interdit l'entrée standard ; transfert borné via `copy-from` et `copy-to`, avec SHA vérifiés avant et après |
| première commande `dpkg-query` interpolée par le shell distant | installation déjà réussie ; affichage rejeté sous `set -u`, requête sans interpolation rejouée et version `0.0.3` confirmée |

## Nettoyage et état final

- Controller B et C, leurs alias IPv4, états, feuilles, sessions et règles
  temporaires sont absents ;
- le coffre Console synthétique et le fichier LAB contenant phrase et code ont
  été déchiquetés ; le paquet Linux reste installé mais non initialisé ;
- les sources de tests, cibles Rust/Node/Go, archives et PKI temporaires ont été
  retirées des runners ;
- les credentials de transit sur Controller, Relay et Daemons ont été
  déchiquetés ;
- les captures et rapports visuels expurgés restent seuls dans le dossier de
  preuve de `lab-console` ;
- Controller A, Relay A et les deux Daemons sont `active`; les listeners produit
  finaux sont `192.168.242.103:9443`, `192.168.243.153:8443` et
  `192.168.243.153:8444` ; aucun listener produit ne reste sur `lab-console` ou
  `lab-machine-2`.

## Limites et passage à Windows

La porte Linux a été exécutée de manière assistée par plusieurs pilotes bornés,
pas par un orchestrateur unique qui produit automatiquement ce rapport. Les
cas unitaires et le scénario hostile vivant couvrent les frontières critiques,
mais chaque perte de réponse, crash de publication, minute d'expiration, borne
de débit et combinaison maximale du contrat n'a pas été provoqué séparément sur
les six VM. Ils restent inventoriés dans
[le registre des tests](../contribution/TESTS.md) au lieu d'être transformés en
réussite implicite.

Lucas a autorisé les commits Linux. Les commits `d8065d4`, `227b075` et
`afb31e8` conservent respectivement l'implémentation, le garde de provenance et
l'ignorance des schémas Tauri générés ; aucun push n'a été effectué. Le candidat
exact a aussi refusé un faux HEAD et un worktree rendu sale avant d'écrire un
manifeste.

Lucas a ensuite autorisé le passage à Windows. La porte native doit encore
prouver le `.msi`, les signatures Authenticode SHA-256 horodatées de
l'exécutable et de l'installateur, l'installation, les ACL du coffre, son IPC,
l'absence de listener et les sept vues sous WebView2. Les contrôles génériques
Linux et Windows pourront s'exécuter en parallèle sur des runners jetables ; la
preuve réseau multi-VM restera hors CI générique sans contrôleur LAB dédié.
Jusqu'à réussite de cette porte distincte, `v0.0.3` reste non terminée malgré la
réussite Linux liée à un commit exact.
