// The fixture bundle's only entry point. It mounts the product's own root
// module, so the stylesheets, the fonts and the React tree are exactly the ones
// the installed Console renders. The build that produces this bundle is the one
// that swaps `@tauri-apps/api/core` for the stand-in next to this file.
import "../../src/main";
